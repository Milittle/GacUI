# AI Prompt 与知识库方法论

## 目的

这份文档总结了一套实用方法，用于在仓库级别构建类似本项目的 AI 协作系统。
它主要解决两个目标：

- 让 AI 的工作方式可预测，而不是临场发挥
- 让项目知识可复用，而不是散落在聊天记录里

## 核心思路

这套系统不应该依赖一条超长总 Prompt。
更好的方式是把职责拆成四层：

1. 请求路由
2. 任务流程 Prompt
3. 领域知识库
4. 持久化任务状态

最后形成的工作流会像这样：

`用户请求 -> 路由到任务类型 -> 执行对应流程 Prompt -> 查询知识库 -> 写入中间任务文档 -> 执行 / 验证`

## 最小可用结构

先从下面这些文件和目录开始：

```text
your-repo/
├─ AGENTS.md
├─ .github/
│  ├─ copilot-instructions.md
│  ├─ prompts/
│  │  ├─ code.prompt.md
│  │  ├─ 2-planning.prompt.md
│  │  ├─ 4-execution.prompt.md
│  │  └─ 5-verifying.prompt.md
│  ├─ KnowledgeBase/
│  │  ├─ Index.md
│  │  ├─ KB_Project_Architecture.md
│  │  └─ KB_Project_APIs.md
│  └─ TaskLogs/
│     ├─ Copilot_Task.md
│     ├─ Copilot_Planning.md
│     └─ Copilot_Execution.md
```

这一版就足够搭出一个真正可用的第一版系统。

## 各层职责

### 1. `AGENTS.md`：请求路由

`AGENTS.md` 负责根据用户请求决定使用哪个流程 Prompt。
它的职责是分类，不是承载项目知识。

常见规则可以是：

- `plan ...` 路由到 planning prompt
- `execute ...` 路由到 execution prompt
- `verify ...` 路由到 verifying prompt
- 其他请求默认走 code prompt

这样可以避免 AI 每次都即兴决定工作流。

### 2. `.github/copilot-instructions.md`：全局操作规则

这个文件定义整个仓库范围内的规则。
它应该回答这些问题：

- 哪些目录允许修改
- 哪些目录是生成文件或受保护目录
- 如何构建
- 如何测试
- 开始工作前必须先读哪些文档
- 做设计决策前是否必须查询知识库

这个文件应该稳定、偏流程约束。
它不应该承载具体任务的执行方案。

### 3. `.github/prompts/*.prompt.md`：流程 Prompt

这些文件描述某一类任务应该怎么执行。

建议最先准备这几类：

- `code.prompt.md`：默认编码流程
- `2-planning.prompt.md`：先写计划，再编码
- `4-execution.prompt.md`：按已批准的计划执行
- `5-verifying.prompt.md`：构建与测试验证

每个 Prompt 文件都应该定义：

- 允许产出什么
- 可以修改哪些文件
- 工作步骤顺序
- 成功标准是什么
- 什么时候该停下，什么时候该继续

把这些文件理解成“流程模板”，而不是“领域知识”。

### 4. `.github/KnowledgeBase/`：领域知识

知识库的目标是减少搜索和猜测。
它不应该试图把整个代码库重写一遍。

最有效的知识类型通常是：

- 模块地图：每个子系统负责什么
- API 选型规则：常见任务该优先用哪个类或模块
- 设计约束：生成代码、反射系统、代码生成依赖、架构边界
- 常见坑点：哪些改动通常会牵出额外更新

保留一个 `Index.md` 作为统一入口。
其余内容拆成小而聚焦的文档。

### 5. `.github/TaskLogs/`：持久化任务状态

这些文件用于把工作中的“短期记忆”外部化。
AI 不再把所有推理都留在聊天上下文里，而是把阶段性结果写入稳定文档。

常见角色如下：

- `Copilot_Task.md`：问题定义或设计结果
- `Copilot_Planning.md`：实施计划
- `Copilot_Execution.md`：执行过程与修复尝试

这能带来三件事：

- 可恢复
- 可审查
- 可追踪

## 最快落地方案

### 第一天：先搭框架

创建：

- `AGENTS.md`
- `.github/copilot-instructions.md`
- `.github/prompts/code.prompt.md`

这一阶段的目标只有一个：
让 AI 不再像通用聊天助手，而开始像一个有仓库规则约束的执行者。

### 第二天：拆开计划与执行

创建：

- `.github/prompts/2-planning.prompt.md`
- `.github/prompts/4-execution.prompt.md`
- `.github/TaskLogs/Copilot_Task.md`
- `.github/TaskLogs/Copilot_Planning.md`
- `.github/TaskLogs/Copilot_Execution.md`

这是收益最大的一步。
一旦“计划”和“执行”分离，整个流程就更容易检查，也更容易纠偏。

### 第三天：加入高价值知识

创建：

- `.github/KnowledgeBase/Index.md`
- 2 到 3 篇聚焦的知识库文档

不要试图一开始就写完整知识库。
只记录那些会真正影响决策的知识。

## 第一批知识库文档应该写什么

如果你一开始只写三篇，建议是下面这些：

### `Index.md`

包含：

- 项目概览
- 主要模块列表
- 所有知识库文档的链接
- “应该先看哪里”的快速指引

### `KB_Project_Architecture.md`

包含：

- 主要入口点
- 核心组件及其职责
- 关键的跨模块流程
- 不应被绕过的边界

### `KB_Project_APIs.md`

包含：

- 常见任务与对应推荐 API / 模块
- 常见反模式
- 替代规则，例如“不要直接调用 X，要通过 Y”

## 这套方法为什么有效

这套结构有效，是因为它把职责清晰分开了：

- Prompt 定义流程
- 知识库定义事实与设计依据
- TaskLogs 定义当前状态
- 全局指令定义约束

如果不这样拆分，AI 要么会被迫过度依赖一条超长 Prompt，要么会在多轮工作中不断丢失上下文。

## 常见失败方式

应避免以下问题：

- 把所有规则都塞进一条巨大的系统 Prompt
- 重要决策只存在于聊天记录中
- 知识库变成随机笔记堆积
- 没有明确 build / test 指令
- 把领域知识和工作流说明混在一起
- 第一版做得过重、过复杂

## 推荐演进路径

### 阶段 1：能用

你拥有：

- 路由机制
- 仓库级说明
- 默认 code prompt
- 一个很小的知识库

这时就已经有明显价值。

### 阶段 2：有纪律

你加入：

- planning prompt
- execution prompt
- verification prompt
- task log 文件

这时整个过程会稳定很多。

### 阶段 3：可运营

你加入：

- 初始化和备份 TaskLogs 的脚本
- review prompt
- investigation、summarize、kb 写作等专项 prompt

这时这套系统就可以支持重复性的团队协作。

### 阶段 4：结构化 Prompt 引擎

你开始把 Prompt 定义放进数据和代码中：

- 可复用的 prompt 变量
- 条件检查
- 工具调用要求
- prompt 校验
- 运行时变量展开

走到这一步后，这套方法就不只是几个 markdown 文件，而会变成一个真正的 Prompt 平台。

## 如何向别人讲解这套方法论

不要把它讲成“提示词工程”。
大多数工程师听到这个词，会以为只是“怎么把 Prompt 写得更花”。
更好的讲法是：
这是一套 AI 辅助研发的工程操作模型。

建议按下面的顺序讲：

### 1. 先讲问题，而不是先讲方案

先描述失败模式：

- AI 回答不稳定
- 仓库规则经常被忘
- 设计决策无法复用
- 团队反复解释同样的事情
- 多轮任务无法顺畅恢复

当别人先认同痛点，再听方法，会更容易理解这套系统存在的必要性。

### 2. 再讲四层模型

把系统简化成四个盒子：

1. 路由
2. 流程 Prompt
3. 知识库
4. 任务状态

这是最容易理解的心智模型。
一开始不要先陷入实现细节。

### 3. 演示一个完整例子

最好用一个真实请求，例如：

`plan add audit logging for billing events`

然后一步一步展示：

- 请求如何被路由
- 激活了哪个 Prompt
- 查询了哪些知识库页面
- 写了哪份任务文档
- 后续如何执行与验证

一个完整例子，通常比十页抽象概念更容易让人理解。

### 4. 重点讲清“职责分离”

必须明确这些边界：

- Prompt 不是知识
- 知识不是任务状态
- 任务状态不是聊天记录
- 仓库规则不是功能设计

这其实就是整套方法的概念核心。

### 5. 用“成熟度模型”来教，而不是一次讲完全部

不要把完整体系当成起点。
更容易落地的讲法是三步：

1. 先做路由和仓库规则
2. 再拆 planning / execution
3. 再加知识库和状态文档

这样别人会觉得这套方法是可采用的，而不是过重的方法学负担。

## 最适合的讲解形式

### 形式 A：架构说明会

适合技术负责人、架构师、资深工程师。

建议结构：

- 问题定义
- 四层模型
- 目录结构示例
- 推行顺序
- 收益与代价

### 形式 B：现场演示

适合产品团队和一线工程师。

挑一个请求，从头走到尾，展示：

- 输入
- 路由规则
- Prompt 文件
- 知识库查询
- 任务文档输出
- 执行与验证流程

通常这是最有说服力的方式。

### 形式 C：内部操作手册

适合团队 onboarding 和规范化落地。

可以写成一份简短手册，回答：

- 有哪些文件
- 每个文件负责什么
- 如何新增一个 Prompt
- 如何新增一篇 KB 文档
- 如何排查工作流失效

### 形式 D：工作坊

适合你希望其他团队也复制这套体系时使用。

建议工作坊流程：

1. 识别参与团队仓库中的一个 AI 协作痛点
2. 设计第一条路由规则
3. 写一个流程 Prompt
4. 写一个 KB 首页
5. 模拟执行一个任务

别人亲手搭出一个很小的版本，比单纯听讲更容易真正掌握。

## 对外分享时的推荐表述

如果你要面向工程师做分享，一个很好的开场表述是：

“我们不是在研究怎么把提示词写得更聪明，我们是在研究如何让 AI 在仓库里更像一个有纪律的工程师。”

然后补一句，这套系统的核心就是把下面四件事显式化：

- 预期显式化
- 流程显式化
- 知识显式化
- 状态显式化

这样的 framing 往往更容易被工程团队接受。

## 最后的建议

如果你要在一个新项目中采用这套方法，不要直接复制成熟系统的全部文件。
应该复制的是它的结构和分层思路，然后实现最小可用版本。

最小但有效的组合是：

- 一个路由文件
- 一个仓库规则文件
- 一个默认 code prompt
- 一个 planning prompt
- 一个很小的知识库首页
- 三个 task log 文件

这已经足够在不制造太多流程负担的情况下，显著提升 AI 协作质量。

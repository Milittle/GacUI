# AI 协作方法论 10 分钟分享提纲

## 目标

这份提纲用于向团队快速介绍一套仓库级 AI 协作方法。
目标不是讲 Prompt 技巧，而是讲如何把 AI 变成一个更有纪律的仓库内协作者。

适合时长：

- 8 到 12 分钟

适合对象：

- 技术负责人
- 架构师
- 一线工程师
- 对 AI 落地方式有疑问的团队成员

## 一句话开场

建议这样开场：

“我们不是在研究怎么把提示词写得更聪明，我们是在研究如何让 AI 在仓库里更像一个有纪律的工程师。”

## 分享结构

### 第 1 分钟：先讲问题

先不要讲方案，先讲团队常见痛点：

- AI 每次做事风格不一致
- 仓库规则经常被忘
- 同一个架构问题需要重复解释
- 多轮工作一断就丢上下文
- AI 产出无法沉淀，也无法复查

这一步的目标是先建立共识：
我们要解决的不是“回答质量偶尔不好”，而是“协作机制不稳定”。

### 第 2 到 3 分钟：讲四层模型

用一张图或四个盒子讲清楚：

1. 请求路由
2. 流程 Prompt
3. 领域知识库
4. 任务状态文档

建议这样解释：

- 请求路由：决定当前请求属于哪类工作
- 流程 Prompt：规定这类工作应该怎么做
- 知识库：提供项目事实、设计依据和 API 选型规则
- 任务状态文档：把阶段结果写下来，让流程可恢复、可追踪

可以直接给出一句总结：

“这套系统的核心，是把预期、流程、知识和状态都显式化。”

### 第 4 到 5 分钟：讲目录结构

给出最小结构示例：

```text
AGENTS.md
.github/copilot-instructions.md
.github/prompts/
.github/KnowledgeBase/
.github/TaskLogs/
```

然后逐个解释：

- `AGENTS.md`：请求分流
- `copilot-instructions.md`：仓库级规则
- `prompts/`：任务流程模板
- `KnowledgeBase/`：领域知识
- `TaskLogs/`：中间状态

这一步不要讲太细，重点是让大家建立“各司其职”的感觉。

### 第 6 到 7 分钟：走一个完整例子

选一个具体例子，例如：

`plan add audit logging for billing events`

按顺序演示：

1. 请求被识别为 `plan`
2. 系统路由到 planning prompt
3. AI 先读取仓库规则和知识库
4. AI 把结果写入 `Copilot_Planning.md`
5. 后续再交给 execution / verify 阶段

这一步非常关键。
一个完整例子，比抽象原则更有说服力。

### 第 8 分钟：讲为什么不要只靠一个大 Prompt

建议直接讲失败模式：

- 太长，难维护
- 新规则容易互相冲突
- 不同任务没有明确边界
- 知识和流程混在一起
- 一旦聊天断掉，中间状态全部丢失

再补一句：

“单一 Prompt 更像一次性指令，分层系统更像工程流程。”

### 第 9 分钟：讲如何低成本起步

强调不要一开始复制完整体系。
建议只做：

- 一个路由文件
- 一个仓库规则文件
- 一个默认 code prompt
- 一个 planning prompt
- 一个知识库首页
- 三个任务状态文档

这一步的目标是降低团队心理门槛。

### 第 10 分钟：收尾

建议用下面这句话收尾：

“如果要让 AI 真正进入工程流程，关键不是把它当聊天工具优化，而是给它一个可执行、可约束、可复用的工作系统。”

## 推荐配图方式

### 图 1：四层模型图

从上到下：

- User Request
- Routing
- Workflow Prompt
- Knowledge Base + Task State
- Execution / Verification

### 图 2：一次请求的流转图

例如：

`plan xxx -> planning prompt -> read KB -> write planning doc -> execute -> verify`

### 图 3：成熟度演进图

分三步即可：

1. 路由 + 规则
2. 计划 + 执行 + 验证
3. 知识库 + 状态沉淀 + 自动化

## 适合在分享中强调的概念

分享时建议重点强调下面几个区分：

- Prompt 不是知识
- 知识不是状态
- 状态不是聊天记录
- 仓库规则不是功能设计

这些区分一旦讲清楚，团队会更容易理解为什么要拆层。

## 常见问答

### Q1：这是不是太重了？

回答建议：

“如果一开始就做完整体系，会显得重；但最小版本只需要几个文件，成本很低，收益很快就能看见。”

### Q2：为什么不用一个写得更好的系统 Prompt？

回答建议：

“因为问题不只是 Prompt 质量，而是流程、知识和状态没有被工程化管理。”

### Q3：知识库是不是会很难维护？

回答建议：

“如果把知识库写成百科会很难维护；但如果只记录会影响决策的高价值知识，维护成本是可控的。”

### Q4：这套东西适合什么团队？

回答建议：

“只要团队已经开始频繁用 AI 参与设计、编码、排查或评审，这套方法都适合。区别只在于做多完整。”

## 演讲建议

演讲时尽量避免：

- 一上来讲 Prompt 细节
- 展示太多文档内容
- 讲过多实现细节
- 让听众误以为这是一个复杂平台工程

更好的方式是：

- 先讲问题
- 再讲结构
- 然后演示一个例子
- 最后讲最小落地方案

## 最后一句

如果这场分享只能让听众记住一句话，那应该是：

“AI 协作要稳定，不靠更花的提示词，而靠更清晰的工程分层。”

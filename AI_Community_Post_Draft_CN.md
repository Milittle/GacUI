# 我不再把 AI 协作当成“写几个 Prompt”，而是把它做成一套工程系统

> 这是一篇预发布草稿。
> 其中包含若干“待替换”与“待补充”位置，用于后续接入我在真实项目中的完整实践。

## 前言

这两年，很多团队都已经开始把 AI 引入研发流程。

但我观察到一个很常见的问题：

大家一开始都以为，所谓“AI 工程实践”，主要就是把 Prompt 写得更好一点。

于是常见做法会变成这样：

- 在某个文档里放几段常用 Prompt
- 在 IDE 里写一段很长的系统提示词
- 遇到新问题时，再临时补几条规则
- 希望 AI 记住这些约定，并在之后持续遵守

这种方式一开始通常是有效的，但只要项目一复杂、协作一变多、任务一跨阶段，很快就会暴露问题：

- AI 的行为不稳定
- 同样的请求，不同轮次表现不一致
- 仓库规则容易被忘
- 设计依据留不下来
- 中间过程无法恢复
- 团队成员很难复用彼此与 AI 的协作经验

后来我逐渐意识到，问题并不只是 Prompt 写得不够好。

真正的问题是：

**我们试图用“聊天提示词”的方式，解决“工程协作系统”的问题。**

所以我开始尝试换一种思路：

不是把 AI 当作一个只能靠上下文记忆的聊天对象，
而是把它放进一个有规则、有流程、有知识入口、有中间状态沉淀的仓库级系统里。

换句话说：

**不是继续优化几段 Prompt，而是把 AI 协作工程化。**

这篇文章会系统介绍这套思路。
它不是某个特定工具的技巧汇总，而是一套可以迁移到不同项目中的方法论。

后续我也会在自己的真实项目中把这套体系完整落地。
文中我已经预留了一些位置，后面会补入实际项目结构、工作流截图、收益数据和踩坑复盘。

---

## 一、问题不在 Prompt，而在协作模型

如果只从“Prompt 工程”角度看 AI 协作，通常会默认几个前提：

- 规则主要靠一段总 Prompt 传达
- 项目知识主要靠 AI 临时阅读代码理解
- 当前任务状态主要依赖会话上下文
- 如果 AI 做错了，就继续补提示词

但在真实工程里，这几个前提都不稳。

### 1. 单一 Prompt 很快会变得不可维护

当你把越来越多规则塞进一条系统提示词里，最终会发生三件事：

- 规则之间互相干扰
- 任务类型之间边界模糊
- 新增约束越来越难验证是否真的生效

最后你得到的不是一个更强的系统，而是一段越来越长、越来越脆弱的文字配置。

### 2. 重要知识留在聊天记录里，本质上不可复用

很多项目里，真正关键的知识不是“某个类怎么用”，而是：

- 哪些目录是生成文件，不能直接改
- 哪些架构边界不能绕过
- 哪种改动一定会触发 codegen / 反射 / schema 更新
- 哪类设计在这个项目里是明确不鼓励的

如果这些知识不被结构化沉淀，而只是分散在聊天里，那么 AI 每一轮都要重新猜，人也很难复用。

### 3. 任务状态只存在上下文里，天然不可靠

只要任务稍微复杂一点，就很容易进入多阶段：

- 先定义问题
- 再做设计
- 再拆计划
- 再执行
- 再验证

如果这些阶段状态都只存在对话里，那么一旦中断：

- 人类要重新解释上下文
- AI 要重新恢复工作状态
- 修改依据和执行过程很难追溯

### 4. 结果是：AI 看起来像“偶尔很强，但整体不稳”

这其实不是模型本身的问题。

更准确地说，是我们给 AI 的工作环境仍然太像一次性聊天，而不像一个工程流程。

所以我更倾向于这样定义问题：

**AI 在工程场景中的不稳定，很多时候不是能力问题，而是运行机制问题。**

---

## 二、我现在更认可的思路：把 AI 协作拆成四层

如果要让 AI 在项目里更像一个“有纪律的工程协作者”，我认为至少要把系统拆成四层：

1. 请求路由
2. 流程 Prompt
3. 领域知识库
4. 任务状态文档

这四层缺一不可。

### 第 1 层：请求路由

请求路由解决的问题是：

**同一句请求，系统应该先进入哪一种工作模式？**

比如下面这些请求，本质上就不应该走同一条流程：

- `plan add audit logging`
- `execute the approved plan`
- `verify the current implementation`
- `review this change`
- `investigate why this fails on Linux`

如果没有路由层，AI 就会每次自行判断。
这会导致：

- 同类任务执行顺序不一致
- 不同阶段的边界模糊
- 复杂任务容易跳步骤

所以路由层的作用，不是提供知识，而是给工作流定入口。

### 第 2 层：流程 Prompt

流程 Prompt 解决的问题是：

**进入某一类任务后，AI 应该按照什么顺序做事？**

例如：

- planning prompt 只允许做计划，不允许直接改代码
- execution prompt 必须按计划执行，并记录修复尝试
- verify prompt 必须围绕构建、测试、结果确认展开

这里最关键的一点是：

**Prompt 的职责应该是定义流程，而不是承载全部项目知识。**

如果把流程和知识混在一起，Prompt 很快就会膨胀，而且越来越难维护。

### 第 3 层：领域知识库

知识库解决的问题是：

**AI 在做设计或代码决策前，应该先看什么，依据什么做判断？**

一个好的知识库，不应该是代码翻译。
它更应该提供这些高价值信息：

- 模块地图：每个子系统负责什么
- API 选型规则：常见需求优先用哪组接口
- 设计约束：哪些边界不能突破
- 常见坑点：哪些改动会牵出额外流程

知识库的价值，不在于“全”，而在于“能缩小判断空间”。

### 第 4 层：任务状态文档

任务状态文档解决的问题是：

**当前阶段的结论、计划、执行过程，应该保存在哪里？**

这层通常是最容易被忽略、但实际价值极高的一层。

因为一旦把阶段状态显式写出来：

- 工作可以恢复
- 过程可以审查
- 经验可以复用
- 失败可以复盘

从这个角度看，任务状态文档其实是在替代“会话上下文”承担更可靠的状态管理职责。

---

## 三、这四层合起来，才是一套真正能落地的 AI 协作系统

如果把它画成一条链路，可以理解成：

`用户请求 -> 路由到任务类型 -> 执行对应 Prompt -> 查询知识库 -> 写入阶段文档 -> 执行 / 验证`

这和“问一句、答一句”的聊天模式有本质区别。

在这个系统里：

- Prompt 不再是唯一载体
- 知识不再依赖临时记忆
- 状态不再只存在会话上下文
- 规则不再散落在个人经验里

我更愿意把它理解为：

**给 AI 提供一个工程化的运行环境，而不是让它在不稳定上下文中自由发挥。**

---

## 四、为什么这套方式比“写几个 Prompt”更强

我觉得主要有五个原因。

### 1. 它把预期显式化了

很多团队之所以觉得 AI “不听话”，并不是因为模型故意不遵守，而是因为：

- 预期没有被明确定义
- 阶段边界没有被明确定义
- 成功标准没有被明确定义

一旦做了流程分层，预期就开始变得具体。

### 2. 它把知识显式化了

没有知识库时，AI 很容易：

- 过度从现有代码表象中归纳规则
- 把偶然实现误当成推荐模式
- 忽略团队真正关心的约束

知识库能显著降低这个问题。

### 3. 它把状态显式化了

这意味着：

- 多轮工作更稳定
- 阶段切换更自然
- 人类更容易接手或复查

这点对复杂任务非常关键。

### 4. 它让团队经验能沉淀，而不是一次性消耗

如果一个团队每次都靠会话临时喂上下文，那么所有积累几乎都不能复用。

而一旦有：

- 路由规则
- Prompt 模板
- 知识库
- 状态文档

你就在不断构建一个可复用的协作系统。

### 5. 它更容易扩展成平台，而不是停留在技巧层

当你继续往前做，就可以自然扩展到：

- Prompt 变量系统
- 条件检查
- 工具调用约束
- 结构化校验
- review / investigate / kb drafting 等专用工作流

这时它就不再只是“提示词集合”，而更像一个真正的工程平台。

---

## 五、如果你想在自己的项目里快速开始，最小版本应该怎么搭

我不建议一开始就复制完整体系。
更好的方式是先搭一个最小可用版本。

最小结构可以是这样：

```text
your-repo/
├─ AGENTS.md
├─ .github/
│  ├─ copilot-instructions.md
│  ├─ prompts/
│  │  ├─ code.prompt.md
│  │  ├─ 2-planning.prompt.md
│  │  ├─ 4-execution.prompt.md
│  │  └─ 5-verifying.prompt.md
│  ├─ KnowledgeBase/
│  │  ├─ Index.md
│  │  ├─ KB_Project_Architecture.md
│  │  └─ KB_Project_APIs.md
│  └─ TaskLogs/
│     ├─ Copilot_Task.md
│     ├─ Copilot_Planning.md
│     └─ Copilot_Execution.md
```

### 最先做的不是知识库，而是分层

如果只能优先做几件事，我建议顺序是：

1. `AGENTS.md`
2. `copilot-instructions.md`
3. 默认 `code.prompt.md`
4. `planning` 与 `execution` prompt
5. 知识库首页
6. 三个状态文档

原因很简单：

先解决流程，再补知识，收益最大。

### 第一版知识库不要写成百科

第一批知识库只要回答几个问题就够了：

- 项目有哪些核心模块
- 哪些 API 是推荐入口
- 哪些目录不能乱改
- 哪些改动会触发额外流程

不要试图一开始就“完整记录项目知识”。
这会把系统做得过重。

### 第一版状态文档只需要极简结构

例如：

- `Copilot_Task.md`
- `Copilot_Planning.md`
- `Copilot_Execution.md`

哪怕最开始每个文件只有一个标题，也没关系。
重点是先把“阶段状态外部化”这件事做起来。

---

## 六、如果你要向团队推广这套方式，应该怎么讲

我不建议一上来就讲 Prompt。

更有效的讲法是：

### 第一步：先讲痛点

比如：

- 为什么 AI 产出不稳定
- 为什么仓库规则总是被忘
- 为什么多人协作很难复用之前的经验
- 为什么复杂任务一旦中断就很难恢复

### 第二步：再讲四层模型

把体系简化成四个概念：

- 路由
- 流程 Prompt
- 知识库
- 状态文档

### 第三步：给一个完整例子

比如：

`plan add audit logging for billing events`

然后一步一步演示：

- 请求如何被识别
- 激活了哪个流程
- 查了哪些知识
- 写了哪份阶段文档
- 后面怎么进入执行与验证

这个例子通常比任何抽象定义都更容易让人理解。

### 第四步：明确告诉团队，这不是“提示词技巧”

我更推荐的表述是：

**这是一套 AI 辅助研发的工程操作模型。**

它不是为了把 Prompt 写得更花，而是为了让 AI 进入一个更接近工程现实的运行环境。

---

## 七、我准备如何在真实项目里落这套体系

接下来我会在自己的实际项目里，把这套方法完整落地。

这里先预留几个后续会补充的部分。

### 1. 项目背景

建议补充内容：

- 项目类型：
- 项目当前阶段：`从 0 到 1 / 持续迭代 / 重构期 / 多人协作维护期`
- 代码规模：
- 主要技术栈：
- 团队规模或协作方式：

建议直接替换为一段 100 到 200 字的项目介绍，重点回答：

- 这是个什么项目
- 为什么复杂到值得引入这套机制
- 目前 AI 主要参与哪些研发活动

可直接填写模板：

> 我正在落地这套体系的项目是【项目名称或类型】。  
> 它目前处于【项目阶段】，主要技术栈是【技术栈】。  
> 这个项目的特点是【复杂度来源，例如模块多、规则多、多人协作、知识密集、长期演进】。  
> 在引入这套机制之前，AI 主要被用于【设计 / 编码 / 排查 / 评审 / 文档整理】，但已经明显暴露出【当前痛点】。

### 2. 为什么这个项目适合引入这套体系

建议补充内容：

- 为什么不能只靠普通 Prompt
- 为什么需要知识沉淀
- 为什么需要中间状态文档
- 为什么这个项目里“规则”比“代码技巧”更重要

可直接填写模板：

> 我选择在这个项目里落地这套体系，主要是因为它具备几个典型特征：  
> 第一，它的任务不是一次性问答，而是经常跨越设计、计划、执行和验证多个阶段。  
> 第二，项目里存在大量不适合靠猜的约束，例如【约束 1】、【约束 2】、【约束 3】。  
> 第三，很多知识是团队经验，如果不沉淀下来，AI 每次都要重新理解。  
> 所以对这个项目来说，单纯优化 Prompt 已经不够，更需要一个可复用的协作系统。

### 3. 第一版目录结构

建议补充内容：

- 真实目录树
- 你最终采用了哪些文件名
- 哪些是第一版就有的，哪些是后续追加的

可直接填写模板：

```text
【在这里替换成你的实际目录结构】
repo/
├─ AGENTS.md
├─ .github/
│  ├─ copilot-instructions.md
│  ├─ prompts/
│  ├─ KnowledgeBase/
│  └─ TaskLogs/
└─ ...
```

建议在目录后补一小段说明：

> 第一版我没有追求完整，而是优先放入了【哪些文件】。  
> 原因是我想先解决【路由 / 流程 / 知识入口 / 状态沉淀】中的哪几个核心问题。

### 4. 第一批 Prompt 与知识库内容

建议补充内容：

- 第一批 Prompt 的种类
- 每个 Prompt 的职责
- 第一批知识库文档主题
- 哪些内容你刻意没有写进 Prompt
- 哪些内容你刻意没有写进知识库

建议结构：

#### 4.1 第一批 Prompt

- `【Prompt 名称】`：负责【职责】
- `【Prompt 名称】`：负责【职责】
- `【Prompt 名称】`：负责【职责】

可直接填写模板：

> 第一批我只保留了最必要的几类 Prompt：  
> `【Prompt A】` 用来处理【任务类型】；  
> `【Prompt B】` 用来处理【任务类型】；  
> `【Prompt C】` 用来处理【任务类型】。  
> 我刻意没有在 Prompt 里塞入大量领域知识，因为我希望 Prompt 只负责流程，而知识交给知识库承载。

#### 4.2 第一批知识库

- `【KB 文档 1】`：记录【内容】
- `【KB 文档 2】`：记录【内容】
- `【KB 文档 3】`：记录【内容】

可直接填写模板：

> 知识库的第一版，我只写了【文档数量】篇，分别覆盖【主题】。  
> 我优先记录的是那些真正会影响判断的内容，例如【例子 1】、【例子 2】、【例子 3】。  
> 反过来，像【某类低价值信息】我没有放进去，因为这些内容直接读代码就能得到。

#### 4.3 删掉了哪些无效做法

可直接填写模板：

> 在第一版设计里，我也刻意删掉了一些看起来“很完整”、但其实低效的做法，例如【做法 1】、【做法 2】、【做法 3】。  
> 原因是这些做法会让系统变重，但并不能明显提升 AI 协作质量。

### 5. 一次真实任务的完整流转

这是后续最值得补的一部分，建议一定写完整。

建议按下面结构补：

#### 5.1 用户请求

```text
【在这里贴真实请求，必要时可脱敏】
```

#### 5.2 系统如何路由

可直接填写模板：

> 这个请求的第一个关键词是 `【关键词】`，所以系统把它路由到了 `【Prompt 文件】`。  
> 这意味着 AI 在这个阶段应该先做【阶段目标】，而不是直接去【不该做的动作】。

#### 5.3 AI 查询了哪些知识

- `【KB 文档】`：因为需要确认【原因】
- `【KB 文档】`：因为需要确认【原因】

#### 5.4 写出了什么阶段文档

可直接填写模板：

> 在这个阶段，AI 先把结果写入了 `【文档名】`。  
> 这份文档主要记录了【内容】。  
> 这样做的价值是【恢复 / 审查 / 复用 / 交接】。

#### 5.5 后续如何进入执行与验证

可直接填写模板：

> 当 planning 阶段完成后，请求继续进入 `【execution / verify】` 阶段。  
> 在这一阶段，系统要求 AI 完成【动作】并记录【状态】。  
> 和普通聊天模式相比，这里最大的差异是【差异点】。

#### 5.6 这次流转里最有价值的地方

可直接填写模板：

> 这次真实任务里，我觉得这套机制最有价值的点不是【表面现象】，而是【真正收益】。  
> 因为它让【具体改进】变得更稳定，也更容易复盘。

### 6. 实际收益

这一节建议尽量写具体，不要只写“感觉更好了”。

可以从下面几个维度选择：

- 上下文恢复成本
- AI 返工率
- 规则遗漏率
- 设计复用率
- 新成员理解成本
- 多阶段任务的稳定性

建议写法：

#### 6.1 定性收益

可直接填写模板：

> 从实际使用感受上，这套机制最明显的改善是【改善 1】、【改善 2】、【改善 3】。  
> 以前最容易出问题的地方是【旧问题】；现在由于【机制变化】，这些问题明显减少了。

#### 6.2 定量指标（如果有）

可直接填写模板：

> 如果从数据上看，目前我观察到的变化包括：  
> - 多阶段任务中断后的恢复时间，从【旧数据】下降到【新数据】  
> - 因规则遗漏导致的返工，从【旧数据】下降到【新数据】  
> - 复用已有设计 / 知识文档的比例，大约达到了【数据】  

如果暂时没有定量数据，也可以写：

> 目前还没有形成严格量化统计，但从连续【周期】的使用情况看，最明显的变化是【变化】。

### 7. 失败与修正

这一节会显著增强文章可信度，建议一定保留。

可从下面几个角度补：

- 哪些 Prompt 一开始写得过长
- 哪些知识后来发现不值得进入 KB
- 哪些流程拆分过细，导致使用成本高
- 哪些规则没有写成硬约束，结果经常失效

可直接填写模板：

> 这套体系并不是一开始就设计对了。  
> 在第一版里，我踩过几个比较典型的坑：  
> - 【坑 1】  
> - 【坑 2】  
> - 【坑 3】  
> 后来我做的修正包括【修正动作】。  
> 这些修正让我越来越确认：这套系统最重要的不是“看起来完整”，而是“分层是否清楚、约束是否有效、维护成本是否可控”。

我很希望这部分不是停留在概念层，而是最终变成一份有真实案例、有取舍依据、有实际收益的数据化复盘。

---

## 八、几个我认为很重要的判断

### 判断 0：它可以算 harness engineering，但不只是 harness engineering

如果要给这套方法找一个更接近行业讨论语境的名字，
我认为它确实可以被归到 `harness engineering` 这个方向里。

因为这套体系的核心，不是继续把能力压进模型内部，
而是通过外部结构来约束模型行为。

这里的外部结构包括：

- 请求如何路由
- 不同任务进入什么流程
- 知识从哪里读取
- 中间状态写到哪里
- 哪些规则必须遵守
- 什么才算任务完成

从这个意义上说，它和很多人理解的“只是写 Prompt”已经不是同一层问题了。

但如果再说得更准确一点，
我觉得这套东西也不应该被狭义地理解成单纯的 harness engineering。

因为它同时还包含了几层东西：

- Prompt Engineering：定义不同任务的流程指令
- Context Engineering：决定给 AI 什么上下文、按什么入口给
- Workflow Engineering：把设计、计划、执行、验证拆成可重复流程
- Knowledge Management：把领域知识沉淀成可查询、可复用的资产
- State Management：把任务状态从会话中外部化

所以我更愿意把它描述为：

**一种面向软件研发流程的 AI Harness Engineering 实践。**

或者说得更通俗一点：

**它是在用工程化方式为 AI 搭建一个稳定的工作系统。**

### 它和 Prompt Engineering 的关系

Prompt Engineering 解决的是：

**怎么把一条或一组提示写得更清楚。**

这当然重要，但它只解决了“指令表达”的问题。

而这套体系进一步解决的是：

- 什么时候该用哪条 Prompt
- Prompt 之外的规则放在哪里
- 领域知识从哪里来
- 多阶段任务怎么切换
- 中间状态怎么保存

所以可以说：

**Prompt Engineering 是这套系统中的一个组成部分，但不是全部。**

### 它和 Context Engineering 的关系

Context Engineering 更关注：

**给模型提供什么上下文，以及如何组织上下文。**

这套体系里，知识库、TaskLogs、路由规则，本质上都在参与 context construction。

但它又不止于“组织上下文”。

因为它还同时规定了：

- 工作流顺序
- 任务边界
- 阶段职责
- 验证方式

所以它比单纯的 context engineering 更偏向“把上下文接入工程流程”。

### 它和 Harness Engineering 的关系

Harness Engineering 更强调：

**通过模型外部的机制，把 AI 行为约束成一个稳定、可复用、可验证的系统。**

从这个定义看，这套方法高度符合 harness engineering 的精神。

尤其是下面这些部分，都是非常典型的 harness 设计：

- 基于请求类型做路由
- 不同任务绑定不同流程 Prompt
- 强制读取仓库规则和知识库
- 使用阶段性文档管理状态
- 执行后必须进入验证

所以如果有人问我：

“这算不算 harness engineering？”

我的回答会是：

**算，而且它比传统意义上的‘写 Prompt’更接近 harness engineering。**

但如果要对外做更通俗的表达，
我通常不会只丢一个术语，
而会说：

**这是一套 AI 协作工程化方法，它的技术内核很接近 harness engineering。**

### 判断 1：Prompt 很重要，但 Prompt 不是全部

很多人讨论 AI 工程实践时，会天然聚焦在 Prompt 上。

但在我看来，Prompt 只是其中一层。

如果没有：

- 规则分层
- 知识入口
- 状态沉淀

再强的 Prompt 也很难稳定支撑复杂协作。

### 判断 2：知识库最重要的不是“全面”，而是“影响决策”

真正值得进知识库的内容，通常不是“代码里能直接看出来的东西”。

而是：

- 哪些模式被鼓励
- 哪些模式要避免
- 哪些改动有隐藏依赖
- 哪些边界不能突破

### 判断 3：任务状态文档是最容易被低估的一层

如果你只做 Prompt 和知识库，不做状态文档，
那么系统依然会在复杂任务上显得不稳。

因为复杂任务真正需要的，不只是规则和知识，
还需要可靠的阶段状态管理。

### 判断 4：最好的起点不是完整系统，而是最小闭环

不要在第一天就做出一个大型平台。

先搭出一个最小但有效的闭环：

- 能分流
- 能查知识
- 能按阶段工作
- 能留状态

只要这四件事成立，系统就已经开始产生价值。

---

## 九、这篇文章后续会怎么更新

这篇文章目前更偏方法论和结构设计。

后续我准备按下面几个方向持续补充：

### 更新方向 1：真实项目落地记录

预留内容：

- 系列文章入口：
- 第一篇案例链接：
- 相关仓库或截图链接：

可直接填写模板：

> 真实项目落地记录我会单独写成系列文章，入口放在这里：  
> 【链接】

### 更新方向 2：Starter Kit 模板

预留内容：

- 模板仓库链接：
- 最小文件集下载地址：
- 示例目录截图：

可直接填写模板：

> 如果后续我把这套体系整理成一个可复用的 Starter Kit，我会把入口补在这里：  
> 【链接】

### 更新方向 3：知识库写作方法

预留内容：

- 如何判断知识是否值得进入 KB
- 如何组织 KB 首页
- 如何避免 KB 写成杂乱笔记

可直接填写模板：

> 关于“什么知识值得进入 KB，什么不值得”，我后面会单独写一篇展开：  
> 【链接】

### 更新方向 4：Prompt 从 markdown 到结构化配置的演进

预留内容：

- Prompt 变量系统
- 条件校验
- 工具调用要求
- 运行时动态变量
- Prompt 配置数据化

可直接填写模板：

> 如果后续我把 Prompt 从 markdown 文件继续演进到结构化配置，我会把相关实践整理在这里：  
> 【链接】

---

## 十、结语

如果让我用一句话总结这套思路，那会是：

**AI 协作要稳定，不靠更花的提示词，而靠更清晰的工程分层。**

我现在越来越倾向于把 AI 看成一个需要被放进工程系统中的协作者。

它当然需要 Prompt。
但它同样需要：

- 明确的入口
- 清晰的流程
- 稳定的知识来源
- 可恢复的任务状态

当这些东西被建立起来之后，AI 才不再只是“偶尔很惊艳”的聊天工具，
而更像一个真正能嵌入项目流程的工程协作单元。

如果你也在尝试把 AI 引入真实研发流程，
也许可以试着不要先问：

“我该怎么写更好的 Prompt？”

而是先问：

**“我能不能先为 AI 设计一个更好的工作系统？”**

---

## 附：可替换与补充清单

下面这些位置建议你在正式发布前替换为真实内容：

- 标题可替换为更适合平台传播的版本
- 前言中的项目背景可补真实案例
- 第七部分可补你自己的项目实践
- 第九部分可补系列文章链接
- 可以补一张“四层模型图”
- 可以补一张“单次任务流转图”
- 可以补一张“最小目录结构图”

建议你正式发布前逐项检查：

- 是否已经补了真实项目背景
- 是否已经补了至少一个完整任务流转案例
- 是否已经把“收益”写具体
- 是否已经保留“失败与修正”部分
- 是否已经删掉不再需要的占位说明

## 附：几个可选标题

你发布时可以从下面这些标题里选，或再改一版：

1. 我不再把 AI 协作当成“写几个 Prompt”，而是把它做成一套工程系统
2. Prompt 不是重点，重点是把 AI 协作工程化
3. 我在项目里搭了一套 AI 协作系统：路由、Prompt、知识库与状态文档
4. 为什么我认为 AI 工程实践的关键，不是 Prompt，而是系统分层
5. 从“写提示词”到“设计工作系统”：我对 AI 协作落地的一次重构

## 附：几个可选结尾 CTA

如果你希望文章更适合社区互动，可以在结尾追加类似一句：

- 如果你也在做类似实践，欢迎交流你们是怎么管理 Prompt、知识和状态的。
- 如果这篇文章后续补完真实项目案例，我会继续更新。
- 如果你对某一层特别感兴趣，例如知识库怎么写，后面我可以单独再展开写。
